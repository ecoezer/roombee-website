import { motion } from 'framer-motion';
import SimpleSlider from './SimpleSlider';

const Hero = () => {
  return (
    <div className="pt-14">
      <SimpleSlider />
    </div>
  );
};

export default Hero;
