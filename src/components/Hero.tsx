import { motion } from 'framer-motion';
import SimpleSlider from './SimpleSlider';

const Hero = () => {
  return (
    <div className="pt-20">
      <SimpleSlider />
    </div>
  );
};

export default Hero;
